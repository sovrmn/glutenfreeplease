<div class="container">
  <div class="row justify-content-center">
    <div class="bg-white p-4 my-4">
    <div class="col-xs-4" id="form">
      <h1>Comment avez-vous trouvé cette recette de <%= @recipe.name %>?</h1>
      <%= simple_form_for [@recipe, @review] do |f| %>
        <%= f.input :content, label: "Vos commentaires"%>
        <%= f.input :rating, collection: (1..5).to_a, label: "Notez cette recette" %>
        <%= f.submit "Partagez votre avis", class: "small-pink-button" %>
      <% end %>
    </div>
    </div>
  </div>
</div>
