<div class=image-main  style="background-image:linear-gradient(rgba(0,0,0,0),rgba(0,0,0,0)), url(https://cdn.dribbble.com/users/2536875/screenshots/12017195/media/cdbaa929738cc6183c1a2c0ef7057539.jpeg);">

</div>

<div class="title-grid">
  <div class="ingredients-title ">Petit index de bons produits sans gluten</div>
  <h2 class="ingredients-subtitle">"C'est avec les meilleurs ingrédients que l'on fait les meilleures recettes"</h2>
</div>

<div class="container-recipe">
  <ul>
    <% ingredient_list = @ingredients.sort_by {|ingredient| ingredient.name}%>
    <% ingredient_list.each do |ingredient| %>
      <li><%= ingredient.name.capitalize %>
          <%= link_to edit_ingredient_path(ingredient) do%>
          <i id="black-ud-icon" class="fas fa-edit ml-3"></i>
          <% end %>
          <%= link_to ingredient_path(ingredient), method: :delete, data: { confirm: "Are you sure?" } do %>
          <i id="black-ud-icon" class="fa fa-trash destroy_dose"></i>
          <% end %>
      </li>
    <% end %>
  </ul>

  <p>
    <%=link_to new_ingredient_path do %>
    <span class="add-recipe">
      <i class="fas fa-plus mr-1">
      </i>
      Ajoutez un ingrédient
    </span>
    <% end %>
  </p>
</div>
